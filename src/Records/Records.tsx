import { Container } from "@mui/material";

const Records = () => {
  return (
    
    <Container
    maxWidth="xs"
    sx={{
      display: 'flex',
      flexDirection: 'column',
      justifyContent: 'center',
      alignItems: 'center',
      minHeight: '100vh',
    }}
  >
    Records
    </Container>
    
  );
}

export default Records;